#!/usr/bin/env sh
set -e

echo "[husky] pre-push: cargo fmt check"
cargo fmt --all -- --check

echo "[husky] pre-push: cargo clippy"
cargo clippy --workspace --all-targets -- -W warnings

echo "[husky] pre-push: cargo test"
cargo test --workspace
