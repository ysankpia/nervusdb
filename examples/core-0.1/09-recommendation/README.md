# 09 Recommendation Seed

Graph shape: `User -LIKES-> Item -IN_CATEGORY-> Category`.

This example writes one two-hop recommendation seed and reads the category
reachable from the selected user.

```bash
bash scripts/core_examples.sh 09-recommendation
```
