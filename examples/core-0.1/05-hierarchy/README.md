# 05 Parent Child Hierarchy

Graph shape: `TreeNode -PARENT_OF-> TreeNode`.

This example writes a directed parent-child edge and reads children from the
root. The query direction proves the child is not returned as a parent.

```bash
bash scripts/core_examples.sh 05-hierarchy
```
