# 02 Dependency Graph

Graph shape: `Package -DEPENDS_ON-> Package`.

This example writes one package dependency and reads the direct dependency for
the selected package name.

```bash
bash scripts/core_examples.sh 02-dependency
```
