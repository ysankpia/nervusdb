# 01 Social Graph

Graph shape: `Person -KNOWS-> Person`.

This example writes one directed social edge and reads Alice's outgoing
neighbor through the CLI.

```bash
bash scripts/core_examples.sh 01-social
```
