name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  rust-ci:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt, clippy

      - name: Cache cargo registry
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: cargo-${{ runner.os }}-${{ hashFiles('**/Cargo.lock') }}

      - name: Check formatting
        run: cargo fmt --all -- --check

      - name: Clippy
        run: cargo clippy --workspace --exclude nervusdb-pyo3 --all-targets -- -W warnings

      - name: TCK Tier-0 (smoke)
        if: runner.os == 'Linux'
        run: make tck-tier0

      - name: TCK Tier-1 (clauses whitelist)
        if: runner.os == 'Linux'
        run: make tck-tier1

      - name: TCK Tier-2 (expressions whitelist)
        if: runner.os == 'Linux'
        run: make tck-tier2

      - name: Binding smoke (Python + Node scaffold)
        if: runner.os == 'Linux'
        run: bash scripts/binding_smoke.sh

      - name: Cross-language contract smoke
        if: runner.os == 'Linux'
        run: bash scripts/contract_smoke.sh

      - name: Run workspace quick tests
        run: bash scripts/workspace_quick_test.sh

      - name: Build release
        run: cargo build --workspace --exclude nervusdb-pyo3 --release
