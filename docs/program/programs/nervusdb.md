# Program: NervusDB Core

- **Vision**: ship a pragmatic embedded knowledge graph with built-in temporal memory.
- **Cadence**: monthly board review, release train every 3 months.
- **Repositories**: `nervusdb/nervusdb` (TypeScript + Rust core, napi bindings).
- **Key Metrics**:
  - Temporal memory fully native by v0.6.0.
  - CI pass rate ≥ 95%.
  - Bench suite (`pnpm bench:temporal`) remains < 1s runtime.
- **Owners**: @luQingU (project), AI assistant (implementation support).
- **Dependencies**: none external (pure local development).
