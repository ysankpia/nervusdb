# Milestone: v0.6.0 – Native Temporal Core

- **Due date**: 2026-05-31
- **Goal**: migrate temporal memory from the TypeScript JSON prototype to the Rust `nervusdb_core` + napi layer.
- **Status**: feature-complete (awaiting final public CI run)

| Issue | Title                                               | Status                                                          | Notes                                                  |
| ----- | --------------------------------------------------- | --------------------------------------------------------------- | ------------------------------------------------------ |
| #35   | [epic] native temporal core                         | 🔄 open – final QA                                              | waiting for last CI run before closing epic            |
| #38   | feat(core): add temporal episode/fact storage       | ✅ merged (#42)                                                 | Rust TemporalStore landed                              |
| #39   | feat(native): expose temporal timeline API          | ✅ merged ([#46](https://github.com/nervusdb/nervusdb/pull/46)) | napi bindings + as_of/between filters shipped          |
| #40   | refactor(ts): rewire temporal memory to native core | ✅ merged ([#45](https://github.com/nervusdb/nervusdb/pull/45)) | TypeScript path now prefers native addon with fallback |
| #41   | docs/chore: native temporal migration guide         | ✅ merged ([#51](https://github.com/nervusdb/nervusdb/pull/51)) | docs/tests/benchmarks published                        |

## Next Actions

1. Convert repo to public once所有 PR ready，跑最后一次 CI（native-matrix + labels snapshot）。
2. 发布 v0.6.0 release notes（引用 `docs/NATIVE_TEMPORAL_MIGRATION.md` 与 benchmarks）。
3. 关闭 epic (#35) 并将 Milestone 状态更新为 "done"。

## Risks / Mitigations

- **Parity gaps**: until TypeScript switches to native path, keep the JSON store as a fallback behind feature flag.
- **Migration**: plan conversion script for existing `.temporal.json` data once native persistence is ready.
- **Addon availability**: ensure loader handles missing native builds gracefully (CI, Windows).
