## TODO: Louvain 第二阶段折叠图构建

范围：`src/algorithms/community.ts` → `buildCommunityGraph()`

背景：当前为简化实现，直接返回原图克隆；需要将同一社区节点合并为“超节点（supernode）”，并折叠边权重。

待办项：

1. 构建社区→超节点映射（nodeId→communityId）
2. 合并节点：
   - 同社区节点聚合为一个超节点（聚合属性可选）。
3. 折叠边：
   - 超节点之间的边权 = 社区间所有边权之和（或平均，作为可选策略）。
4. 更新模块度计算输入：
   - 使用折叠图作为下一层优化输入。

验收标准：

- 小图验证与模块度单测一致；
- 折叠前/后层级统计（节点/边计数）符合预期；
- 与第一阶段组合后收敛稳定且不退化。
